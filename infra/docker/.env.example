# UrbanFlow — Docker Compose environment variables
# Copy to .env in this directory if you need custom values:
#   copy .env.example .env

POSTGRES_USER=urbanflow
POSTGRES_PASSWORD=urbanflow
POSTGRES_DB=postgres
POSTGRES_PORT=5432

REDIS_PORT=6379

KAFKA_PORT=9092
